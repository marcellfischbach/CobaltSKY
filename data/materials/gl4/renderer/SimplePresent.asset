asset() {
	data() {
		program(name:"SimplePresent") {
			techniques() {
				technique(name:"Default") {
					shader(locator:"${shaders}/renderer/simple_present.vert")
					shader(locator:"${shaders}/renderer/simple_present.frag")
				}
			}
		}
	}
}