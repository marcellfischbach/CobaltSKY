asset() {
	data() {
		program(name:"ArrayPresent") {
			techniques() {
				technique(name:"Default") {
					shader(locator:"${shaders}/renderer/array_present.vert")
					shader(locator:"${shaders}/renderer/array_present.frag")
				}
			}
		}
	}
}